import { NextRequest } from "next/server";
import { db } from "@/server/db/client";import { requireCsrf } from "@/server/http/account-action";import { authenticated,errorResponse,privateResponse } from "@/server/http/responses";
export async function POST(request:NextRequest,{params}:{params:Promise<{targetId:string}>}){const csrf=requireCsrf(request);if(csrf)return csrf;const auth=await authenticated(request);if("response" in auth)return auth.response;const targetId=Number((await params).targetId);const rows=await db()<Array<{id:number}>>`UPDATE tracked_targets SET discord_webhook_url=NULL,webhook_kind='discord',discord_last_error=NULL,discord_last_sent_at=NULL,updated_at=now() WHERE id=${targetId} AND user_id=${auth.user.id} RETURNING id`;if(!rows.length)return errorResponse(404,"NOT_FOUND","That project doesn't exist.",request);return privateResponse({data:{configured:false}},request);}
