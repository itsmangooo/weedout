import { NextRequest } from "next/server";import { machineAuthenticated,machineError,machineResponse } from "@/server/auth/machine";import { deleteMachineProject,MachineProjectError } from "@/server/repositories/machine-projects";
export async function DELETE(request:NextRequest,{params}:{params:Promise<{projectId:string}>}){const auth=await machineAuthenticated(request);if("response" in auth)return auth.response;try{await deleteMachineProject(auth.identity.user_id,Number((await params).projectId));return machineResponse({deleted:true});}catch(error){if(error instanceof MachineProjectError)return machineError(error.status,error.code,error.message);throw error;}}
